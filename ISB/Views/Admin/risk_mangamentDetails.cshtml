@model ISB.Models.mainModel;
@if (TempData["msg1"] != null)
{
    <script>alert('@TempData["msg1"]');</script>
}
<div class="content" id="content">

    <div class="title">Risk Management</div>
    <div class="search-bar">
        <select>
            <option value="all">All</option>
        </select>
        <form method="get" class="d-flex">
            <input placeholder="Search" type="text" name="search_text" />
            <button class="btn-search">Search</button>
        </form>
        <div class="container text-end">
           
            <a class="btn btnadd bg-blue-500 hover:bg-blue-700 " href="@Url.Action("addRisk_Mangement","admin")">Add Risk Management</a>
        </div>
    </div>


    <table class="table">
        <thead>
            <tr>
                <th>
                    Tittle
                </th>
                <th>
                    Stakeholder Engagement
                </th>
                <th>
                    Communication Effectiveness
                </th>
                <th class="action">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var data in Model.riskmangament_details)
            {
                <tr>

                    <td>@data.IdentifiedRisks</td>
                    <td>@data.Stakeholder_Engagement.name</td>
                    <td>@data.Effectiveness_Engagement.name</td>


                    <td class="action">
                        <i class="fas fa-ellipsis-v action-icon"></i>
                        <ul class="action-menu">
                            <li><a href="@Url.Action("update_riskmang","admin",new{id = data.id})"><i class="fas fa-edit"></i>Edit</a></li>
                            <li><a href="@Url.Action("view_riskmang","admin",new{id= data.id})"><i class="fa-solid fa-eye"></i>View</a></li>

                            <li><a href="@Url.Action("delete_riskmang","admin",new{id = data.id})"><i class="fas fa-trash"></i>Delete</a></li>
                        </ul>
                    </td>
                </tr>
            }

            @if (TempData["msg"] != null)
            {
                <tr>
                    <td class="text-center" colspan="5">@TempData["msg"]</td>
                </tr>
            }

        </tbody>
    </table>


</div>


<style>
    body {
        background-color: #f8f9fa;
    }

    .modal-dialog {
        max-width: 900px;
    }

    .modal-header {
        border-bottom: none;
    }

    .modal-footer {
        border-top: none;
    }

    .form-control {
        margin-bottom: 1rem;
    }

    .form-check-inline {
        margin-right: 1rem;
    }

    .container {
        margin-top: 50px;
    }

    .modal-body {
        background-color: #fff;
    }

    .modal-footer button {
        width: 100px;
    }

    .btnadd {
        padding: 10px 20px;
       
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

        .btnadd:hover {
            padding: 10px 20px;
           color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

</style>
